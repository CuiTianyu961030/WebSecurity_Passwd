此文件夹是对上级文件夹的补充。
alpha-----针对原始密码库，里面包含不同长度的字母串的文件，每个文件内容为相同长度（长度为文件名）的字母串及其概率
char---- -针对原始密码库，里面包含不同长度的字符串的文件，每个文件内容为相同长度（长度为文件名）的字符串及其概率
digit-----针对原始密码库，里面包含不同长度的数字串的文件，每个文件内容为相同长度（长度为文件名）的数字串及其概率

alphaV-----针对生成密码库，里面包含不同长度的字母串的文件，每个文件内容为相同长度（长度为文件名）的字母串及其概率（用以与alpha作对比）
charV---- -针对生成密码库，里面包含不同长度的字符串的文件，每个文件内容为相同长度（长度为文件名）的字符串及其概率（用以与digit作对比）
digitV-----针对生成密码库，里面包含不同长度的数字串的文件，每个文件内容为相同长度（长度为文件名）的数字串及其概率（用以与char作对比）

sample-----上述文件夹中的文件之一，此文件为digit中文件名为5.txt的文件，表示数字串中长度为5的各个数字串的概率
